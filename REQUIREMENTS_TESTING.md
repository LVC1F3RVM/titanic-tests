# **🛠️ Требования для тестирования и устранение проблем**

Этот документ содержит инструкции по настройке окружения для запуска автотестов проекта Titanic Microservices API, а также решения типичных проблем.

## **✅ Требования к системе**

Перед началом работы убедитесь, что у вас установлены:

1. **Docker & Docker Compose** — для запуска микросервисов.  
2. **Node.js** — среда выполнения для тестов (рекомендуется версия **LTS**).  
3. **npm** — пакетный менеджер (устанавливается автоматически с Node.js).

## **💻 Установка и проверка Node.js**

### **1\. Проверка установки**

Откройте терминал и выполните команды:

node \-v  
npm \-v

Если вы видите номера версий (например, v20.10.0 и 10.2.3), значит, всё готово к работе.

### **2\. Установка**

Если команды не найдены:

1. Перейдите на [официальный сайт Node.js](https://nodejs.org/).  
2. Скачайте версию **LTS** (Long-Term Support) для вашей ОС.  
3. Установите, следуя инструкциям инсталлятора.

## **❌ Решение проблем с PowerShell (Windows)**

При попытке запустить скрипты (например, npm init или npx playwright) на Windows вы можете столкнуться с ошибкой:

npm : File C:\\Program Files\\nodejs\\npm.ps1 cannot be loaded because running scripts is disabled on this system.  
PSSecurityException

Это происходит из\-за строгой **Политики выполнения скриптов (Execution Policy)** в PowerShell.

### **🔑 Решение**

Вам нужно временно изменить политику выполнения.

1. **Запустите PowerShell от имени Администратора**:  
   * Нажмите Win, введите PowerShell.  
   * ПКМ \-\> "Запуск от имени администратора".  
2. **Введите команду**:  
   Set-ExecutionPolicy RemoteSigned

   *Эта политика разрешает запуск локальных скриптов без подписи, что безопасно для разработки.*  
3. **Подтвердите действие**:  
   * Нажмите Y (Yes), затем Enter.  
4. **Перезапустите терминал**:  
   * Закройте окно администратора и вернитесь в ваш обычный терминал (VS Code или cmd). Ошибка должна исчезнуть.

## **🏗️ Настройка инфраструктуры Playwright**

Пошаговая инструкция по созданию проекта для тестов с нуля.

### **Шаг 1: Инициализация проекта**

Создайте папку и инициализируйте npm-проект:

mkdir titanic-tests  
cd titanic-tests  
npm init \-y

### **Шаг 2: Установка Playwright**

Установите фреймворк и инициализируйте конфигурацию:

\# Установка зависимостей  
npm install \-D @playwright/test typescript @types/node

\# Инициализация Playwright (выберите TypeScript при установке)  
npx playwright init

### **Шаг 3: Настройка TypeScript**

Убедитесь, что файл tsconfig.json имеет следующие настройки для корректной работы импортов:

{  
  "compilerOptions": {  
    "target": "ES2020",  
    "module": "CommonJS",  
    "rootDir": "./",  
    "moduleResolution": "node",  
    "outDir": "./dist",  
    "esModuleInterop": true,  
    "forceConsistentCasingInFileNames": true,  
    "strict": true,  
    "skipLibCheck": true,  
    "sourceMap": true,  
    "allowJs": true,  
    "resolveJsonModule": true  
  },  
  "include": \["\*\*/\*.ts", "\*\*/\*.js"\]  
}

### **Шаг 4: Константы проекта**

Создайте файл src/config/constants.ts для хранения URL и тестовых данных:

export const BASE\_URL \= 'http://localhost:8000';  
export const AUTH\_SERVICE\_URL \= 'http://localhost:8003';

export const ADMIN\_CREDENTIALS \= {  
  username: 'captain',  
  password: 'ship123',  
  email: 'captain@titanic.com',  
};

export const NEW\_PASSENGER \= {  
  name: "Smith, Mr. Test",  
  pclass: 2,  
  sex: "male",  
  age: 30,  
  fare: 25.50,  
  embarked: "Southampton",  
  destination: "New York",  
  ticket: "TEST-123"  
};

## **🚀 Запуск тестов**

Убедитесь, что микросервисы запущены (docker-compose up), и выполните:

\# Запуск всех тестов  
npx playwright test

\# Запуск с визуальным интерфейсом (UI mode)  
npx playwright test \--ui

\# Запуск конкретного файла  
npx playwright test tests/auth.spec.ts  
