# **🧪 Полное руководство по тестированию Titanic Microservices API**

Этот документ является **единым справочником** по всем видам автоматизированного тестирования, реализованным в проекте. Здесь описана логика тестов, их структура и особенности запуска для каждого микросервиса.

## **📋 Содержание**

- [1. Введение и Запуск](#1-введение-и-запуск)  
- [2. API Gateway (Интеграционные тесты)](#2-api-gateway-api-gatewayspects)  
- [3. Auth Service (Безопасность)](#3-auth-service-auth-servicespects)  
- [4. Passenger Service (Бизнес-логика)](#4-passenger-service-passenger-servicespects)  
- [5. Statistics Service (Аналитика)](#5-statistics-service-statistics-servicespects)  
- [6. Resilience Testing (Хаос-тестирование)](#6-resilience-testing-resilience-loopspects)  
- [7. Решение проблем](#7-решение-проблем)

## **1\. Введение и Запуск**

Тесты написаны на **TypeScript** с использованием фреймворка **Playwright**. Они проверяют работу системы "снаружи", отправляя реальные HTTP-запросы к запущенным Docker-контейнерам.

### **Предварительные требования**

* **Docker**: Контейнеры должны быть запущены (docker-compose up).  
* **Node.js**: Установлен локально.

### **Команды запуска**

\# 🚀 Запустить ВСЕ тесты  
npx playwright test

\# 🖥️ Запустить с интерфейсом (для отладки)  
npx playwright test \--ui

\# 📂 Запустить тесты конкретного сервиса  
npx playwright test tests/auth\_service/

## **2\. API Gateway (api-gateway.spec.ts)**

**Файл:** tests/api\_gateway/api-gateway.spec.ts

**Цель:** Проверка маршрутизации, здоровья системы и сквозных сценариев (E2E).

### **Ключевые особенности**

1. **Глобальный хук Admin (beforeAll):**  
   * *Проблема:* Только первый зарегистрированный пользователь становится Админом. Если другие тесты запустятся раньше, тестовый юзер станет обычным пользователем без прав на удаление.  
   * *Решение:* Тест принудительно регистрирует admin перед запуском всех сценариев.  
2. **Последовательный поток (Sequential Flow):**  
   * Сценарий Full Passenger Lifecycle выполняет операции строго по порядку: Create \-\> Search \-\> Update \-\> Delete. Это предотвращает ошибки 422, когда ID еще не создан.

### **Сценарии**

* **Health Check:** Проверяет статус /health (должен быть "healthy" у всех 4 сервисов).  
* **Auth Proxy:** Проверяет проксирование регистрации, логина и выхода.  
* **Lifecycle:** Полный цикл жизни пассажира (создание, поиск, удаление админом).

## **3\. Auth Service (auth-service.spec.ts)**

**Файл:** tests/auth\_service/auth-service.spec.ts

**Цель:** Проверка безопасности, валидации и управления токенами.

### **Ключевые особенности**

1. **Уникальные пользователи:**  
   * Используется функция generateUser() с timestamp, чтобы избежать ошибок "User already exists" при повторных запусках.  
2. **Задержка токенов:**  
   * В тесте /refresh добавлена пауза 1.5 сек, чтобы временная метка exp (expiration) в новом токене гарантированно отличалась от старого.  
3. **Гибридная проверка:**  
   * Health check проверяется напрямую через порт сервиса, а бизнес-логика — через Gateway.

### **Сценарии**

* **Registration:** Валидация дубликатов и коротких паролей.  
* **Token Rotation:** Обновление Access-токена через Refresh-токен.  
* **Revocation (Logout):** Критический тест. Проверяет, что после логаута старый Refresh-токен попадает в черный список.

## **4\. Passenger Service (passenger-service.spec.ts)**

**Файл:** tests/passenger\_service/passenger-service.spec.ts

**Цель:** Проверка CRUD, прав доступа (RBAC) и пасхалок.

### **Ключевые особенности**

1. **Serial Mode:**  
   * Блок Secure CRUD Operations помечен как test.describe.configure({ mode: 'serial' }), чтобы гарантировать порядок выполнения зависимых тестов.  
2. **Разделение ролей:**  
   * Используются два токена: userToken (для создания/обновления) и adminToken (для удаления). Тест проверяет, что User получает 403 при попытке удаления.  
3. **Пасхалки:**  
   * Тест проверяет точный текст ошибки при попытке поселить Джека и Розу в одну каюту: *"Different social classes cannot share cabins..."*.

### **Сценарии**

* **Public Read:** Пагинация, фильтрация по классу/полу.  
* **RBAC:** Проверка запрета на удаление для обычных пользователей.  
* **Easter Egg:** Логика "Jack & Rose".

## **5\. Statistics Service (statistics-service.spec.ts)**

**Файл:** tests/statistics\_service/statistics-service.spec.ts

**Цель:** Проверка агрегации данных и математических расчетов.

### **Ключевые особенности**

* **Проверка типов (Contract):** Тесты проверяют типы полей (number, string), а не только их значения, чтобы гарантировать стабильность API для фронтенда.  
* **Математическая проверка:** В тесте распределения по возрасту проверяется, что сумма процентов всех групп равна \~100%.

### **Зависимости**

⚠️ **Важно:** Сервис статистики зависит от Passenger Service. Если сервис пассажиров недоступен, тесты статистики упадут с ошибкой 503\.

## **6\. Resilience Testing (resilience-loop.spec.ts)**

**Файл:** tests/advanced/resilience-loop.spec.ts

**Цель:** Проверка устойчивости системы (Chaos Engineering).

### **Инструкция по запуску (Интерактивный тест)**

Этот тест проверяет, как ведет себя система при падении зависимости.

1. Уберите .skip в коде теста.  
2. Запустите тест: npx playwright test tests/advanced/resilience-loop.spec.ts  
3. Во втором терминале выполните: docker stop passenger-service  
4. Наблюдайте за логами:  
   * ✅ **Graceful:** Статусы 503/502 (сервис обработал ошибку).  
   * ❌ **Crash:** Статусы 500 или зависание (сервис упал).

## **7\. Решение проблем**

| Ошибка | Возможная причина | Решение |
| :---- | :---- | :---- |
| **403 Forbidden (DELETE)** | Тестовый юзер не стал админом. | Перезапустите контейнеры (docker-compose restart), чтобы сбросить базу и позволить beforeAll хуку зарегистрировать админа первым. |
| **422 Unprocessable Entity** | id пассажира равен undefined. | Убедитесь, что тесты CRUD запускаются в режиме serial или объединены в один тест. |
| **Connection Refused** | Контейнеры не запущены. | Выполните docker-compose up \-d. |
| **User already exists** | Конфликт имен в тесте. | Убедитесь, что используете генераторы уникальных имен (Date.now()). |

